"use client";
import {useEffect,useState} from "react";
import {binanceSymbol} from "../../lib/market-data";
type Book={bids:[string,string][];asks:[string,string][]};
export default function FuturesMarketData({pair}:{pair:string}){const [book,setBook]=useState<Book>({bids:[],asks:[]});const [trades,setTrades]=useState<any[]>([]);const [ticker,setTicker]=useState<any>({});useEffect(()=>{let stop=false;const symbol=binanceSymbol(pair);const load=async()=>{try{const [d,t,k]=await Promise.all([fetch(`https://fapi.binance.com/fapi/v1/depth?symbol=${symbol}&limit=12`).then(r=>r.json()),fetch(`https://fapi.binance.com/fapi/v1/aggTrades?symbol=${symbol}&limit=20`).then(r=>r.json()),fetch(`https://fapi.binance.com/fapi/v1/ticker/24hr?symbol=${symbol}`).then(r=>r.json())]);if(!stop){setBook({bids:d.bids||[],asks:d.asks||[]});setTrades(t||[]);setTicker(k||{})}}catch{}};load();const id=setInterval(load,3000);return()=>{stop=true;clearInterval(id)}},[pair]);return <><style>{`.futures-data-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}.market-data-panel{padding:22px!important;border:1px solid #20364d!important;border-radius:16px!important;background:#0a1523!important;min-width:0}.market-data-panel h2{margin:7px 0 0;font-size:22px}.market-data-panel .section-heading{display:flex;align-items:flex-start;justify-content:space-between}.book-head,.trades-head,.book-side>div,.trade-line{display:grid;grid-template-columns:1fr 1fr;gap:12px;align-items:center;font-size:12px}.book-head,.trades-head{padding:10px 0;color:#6f849b;border-bottom:1px solid #203247}.book-side>div,.trade-line{padding:6px 0;color:#a9bbce}.book-side>div span:last-child,.trade-line span:nth-child(2),.trade-line span:last-child{text-align:right}.book-side.asks span:first-child{color:#fb7185}.book-side.bids span:first-child{color:#34d399}.book-mid{margin:6px 0;padding:8px;text-align:center;border:1px dashed #29435e;border-radius:8px;color:#7f96ae;font-size:11px}.trades-head,.trade-line{grid-template-columns:1fr 1fr 1fr}.trade-line{border-bottom:1px solid #14263a}.market-data-panel .tiny{padding-top:4px}@media(max-width:760px){.futures-data-grid{grid-template-columns:1fr;gap:14px}.market-data-panel{padding:18px!important;border-radius:12px!important}}`}</style><div className="futures-data-grid"><section className="panel market-data-panel"><div className="section-heading"><div><div className="label">MARKET DEPTH</div><h2>Order book</h2></div><span className="muted tiny">Live</span></div><div className="book-head"><span>Price (USDT)</span><span>Amount</span></div><div className="book-side asks">{book.asks.slice(0,6).reverse().map((x,i)=><div key={i}><span>{Number(x[0]).toFixed(2)}</span><span>{Number(x[1]).toFixed(4)}</span></div>)}</div><div className="book-mid">Spread {book.asks[0]&&book.bids[0]?((+book.asks[0][0])-(+book.bids[0][0])).toFixed(2):"—"}</div><div className="book-side bids">{book.bids.slice(0,6).map((x,i)=><div key={i}><span>{Number(x[0]).toFixed(2)}</span><span>{Number(x[1]).toFixed(4)}</span></div>)}</div></section><section className="panel market-data-panel"><div className="section-heading"><div><div className="label">RECENT ACTIVITY</div><h2>Recent trades</h2></div><span className="muted tiny">24h {ticker.priceChangePercent?`${Number(ticker.priceChangePercent).toFixed(2)}%`:"—"}</span></div><div className="trades-head"><span>Price</span><span>Quantity</span><span>Time</span></div>{trades.slice(0,10).map((t,i)=><div className={`trade-line ${t.isBuyerMaker?"down":"up"}`} key={i}><span>{Number(t.p).toFixed(2)}</span><span>{Number(t.q).toFixed(4)}</span><span>{new Date(t.T||Date.now()).toLocaleTimeString()}</span></div>)}</section></div></>}
