"use client";
import {FormEvent, useState} from "react";
import Link from "next/link";
export default function Signup(){const [name,setName]=useState("");const [email,setEmail]=useState("");const [password,setPassword]=useState("");const submit=(e:FormEvent)=>{e.preventDefault();alert("Account creation will be connected to Supabase later.")};return <main className="auth-page"><div className="auth-card"><Link className="brand" href="/">ARBITRAGE<span>.</span></Link><div className="eyebrow">CREATE ACCOUNT</div><h1>Join the exchange</h1><p className="muted">Create your account and start with testnet funds.</p><form onSubmit={submit}><label>Full name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimum 8 characters"/></label><button className="btn primary full" type="submit">Create account</button></form><p className="auth-foot">Already registered? <Link href="/login">Log in</Link></p></div></main>}