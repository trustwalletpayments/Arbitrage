"use client";
import {FormEvent,useState} from "react";import Link from "next/link";import {getBalances,setBalances,addTransaction} from "../../../lib/wallet-store";
export default function Transfer(){const [done,setDone]=useState(false);const [amount,setAmount]=useState("");const [from,setFrom]=useState("Spot wallet");const [to,setTo]=useState("Futures wallet");const [error,setError]=useState("");const submit=(e:FormEvent)=>{e.preventDefault();setError("");const n=Number(amount);if(from===to){setError("From and To wallets must be different.");return}if(!Number.isFinite(n)||n<=0){setError("Enter a valid amount.");return}const b=getBalances();const source=from==="Spot wallet"?"spot":"futures";const target=to==="Spot wallet"?"spot":"futures";if(b[source]<n){setError(`Insufficient ${from.toLowerCase()} balance.`);return}const next={...b,[source]:b[source]-n,[target]:b[target]+n};setBalances(next);addTransaction({id:crypto.randomUUID(),type:"TRANSFER",asset:"USDT",amount:n,from,to,status:"COMPLETED",createdAt:new Date().toISOString()});setDone(true)};return <main className="app-shell"><header className="appbar"><Link className="brand" href="/">ARBITRAGE<span>.</span></Link><nav><Link href="/dashboard">Dashboard</Link><Link href="/trade">Spot</Link><Link href="/futures">Futures</Link><Link className="active" href="/wallet">Wallet</Link><Link href="/orders">Orders</Link></nav><Link className="btn" href="/wallet">Back to wallet</Link></header><section className="app-content"><div className="auth-card" style={{margin:"40px auto",width:"min(520px,100%)"}}><div className="eyebrow">INTERNAL TRANSFER</div><h1>Move funds</h1><p className="muted">Transfer USDT between your testnet spot and futures wallets.</p>{done?<div className="notice"><strong>Transfer completed.</strong><p className="muted">${Number(amount).toFixed(2)} USDT moved from {from} to {to}.</p><Link className="btn primary" href="/wallet">Return to wallet</Link></div>:<form onSubmit={submit}><label>From<select value={from} onChange={e=>setFrom(e.target.value)}><option>Spot wallet</option><option>Futures wallet</option></select></label><label>To<select value={to} onChange={e=>setTo(e.target.value)}><option>Futures wallet</option><option>Spot wallet</option></select></label><label>Asset<select disabled><option>USDT</option></select></label><label>Amount<input required min="0" type="number" step="any" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="0.00"/></label>{error&&<div className="notice danger">{error}</div>}<button className="btn primary full" type="submit">Transfer {amount||"funds"}</button><p className="muted tiny">Testnet only. This updates browser storage, not a production ledger.</p></form>}</div></section></main>}
