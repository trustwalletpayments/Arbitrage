"use client";
import {FormEvent,useState} from "react";import Link from "next/link";
export default function Transfer(){const [done,setDone]=useState(false);const [amount,setAmount]=useState("");const submit=(e:FormEvent)=>{e.preventDefault();setDone(true)};return <main className="app-shell"><header className="appbar"><Link className="brand" href="/">ARBITRAGE<span>.</span></Link><nav><Link href="/dashboard">Dashboard</Link><Link href="/trade">Spot</Link><Link href="/futures">Futures</Link><Link className="active" href="/wallet">Wallet</Link></nav><Link className="btn" href="/wallet">Back to wallet</Link></header><section className="app-content"><div className="auth-card" style={{margin:"40px auto",width:"min(520px,100%)"}}><div className="eyebrow">INTERNAL TRANSFER</div><h1>Move funds</h1><p className="muted">Transfer available funds between your spot and futures wallets.</p>{done?<div className="notice"><strong>Transfer request created.</strong><p className="muted">This is a testnet UI preview. No balance has been changed.</p><Link className="btn primary" href="/wallet">Return to wallet</Link></div>:<form onSubmit={submit}><label>From<select defaultValue="Spot wallet"><option>Spot wallet</option><option>Futures wallet</option></select></label><label>To<select defaultValue="Futures wallet"><option>Futures wallet</option><option>Spot wallet</option></select></label><label>Asset<select><option>USDT</option><option>BTC</option><option>ETH</option></select></label><label>Amount<input required min="0" type="number" step="any" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="0.00"/></label><button className="btn primary full" type="submit">Transfer {amount||"funds"}</button><p className="muted tiny">Testnet preview only. The real ledger will be connected later.</p></form>}</div></section></main>}
