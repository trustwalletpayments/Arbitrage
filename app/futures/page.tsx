"use client";
import {useEffect,useMemo,useState} from "react";
import Link from "next/link";
import MarketChart from "../components/MarketChart";
import CoinIcon from "../components/CoinIcon";
import {MARKET_SYMBOLS,displayPair,binanceSymbol,formatPrice} from "../../lib/market-data";
import {addTestnetPosition,getTestnetPositions,updateTestnetPosition} from "../../lib/testnet-store";
import type {StoredPosition} from "../../lib/testnet-store";
import {getTestnetBalances,setTestnetBalances} from "../../lib/testnet-wallet";
import "../futures-page.css";
import "../futures-mobile.css";

function validPair(value:string|null){const compact=(value||"BTCUSDT").toUpperCase().replace(/[^A-Z0-9]/g,"");return MARKET_SYMBOLS.includes(compact)?displayPair(compact):"BTC/USDT"}
function priceFrom(data:any){const value=Number(data?.p??data?.markPrice??data?.price);return Number.isFinite(value)&&value>0?value:0}

export default function Futures(){
 const [pair,setPair]=useState("BTC/USDT"),[expanded,setExpanded]=useState(false),[side,setSide]=useState("Long"),[lev,setLev]=useState(10),[orderType,setOrderType]=useState("Limit"),[entryPrice,setEntryPrice]=useState(""),[quantity,setQuantity]=useState(""),[takeProfit,setTakeProfit]=useState(""),[stopLoss,setStopLoss]=useState(""),[markPrice,setMarkPrice]=useState(0),[positions,setPositions]=useState<StoredPosition[]>([]),[message,setMessage]=useState(""),[submitting,setSubmitting]=useState(false);
 const symbol=binanceSymbol(pair),base=pair.split("/")[0];
 useEffect(()=>{setPair(validPair(new URLSearchParams(window.location.search).get("pair")));setPositions(getTestnetPositions())},[]);
 useEffect(()=>{
  let cancelled=false,ws:WebSocket|undefined,timer:ReturnType<typeof setInterval>|undefined;
  setMarkPrice(0);setEntryPrice("");
  const apply=(value:number)=>{if(!cancelled&&value>0){setMarkPrice(value);setEntryPrice(x=>x||String(value));setPositions(current=>current.map(p=>{if(p.status!=="OPEN")return p;const pnl=(p.side==="LONG"?value-p.entryPrice:p.entryPrice-value)*p.quantity;updateTestnetPosition(p.id,{markPrice:value,unrealizedPnl:pnl});return {...p,markPrice:value,unrealizedPnl:pnl}}))}};
  const poll=async()=>{try{const r=await fetch(`/api/testnet/futures?symbol=${encodeURIComponent(symbol)}&t=${Date.now()}`,{cache:"no-store"});const data=await r.json();if(r.ok)apply(priceFrom(data))}catch{}};
  poll();timer=setInterval(poll,3000);
  try{ws=new WebSocket(`wss://fstream.binance.com/ws/${symbol.toLowerCase()}@markPrice@1s`);ws.onmessage=e=>{try{apply(priceFrom(JSON.parse(e.data)))}catch{}};ws.onclose=()=>{if(!cancelled)poll()}}catch{}
  return()=>{cancelled=true;if(timer)clearInterval(timer);try{ws?.close()}catch{}}
 },[symbol]);
 const livePnl=useMemo(()=>{const e=Number(entryPrice),q=Number(quantity);return e>0&&q>0&&markPrice>0?(side==="Long"?markPrice-e:e-markPrice)*q:0},[entryPrice,quantity,side,markPrice]);
 async function submit(){setMessage("");const entry=orderType==="Market"?markPrice:Number(entryPrice),qty=Number(quantity);if(entry<=0||qty<=0){setMessage("Live price is still loading. Enter a valid price and quantity.");return}const margin=entry*qty/lev,balances=getTestnetBalances(),wallet=balances.find(x=>x.asset==="USDT");if(!wallet||wallet.futures<margin){setMessage(`Insufficient futures balance. Required margin: ${margin.toFixed(2)} USDT`);return}setSubmitting(true);try{const r=await fetch("/api/testnet/futures",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({symbol:pair,side:side.toUpperCase(),entryPrice:entry,markPrice,quantity:qty,leverage:lev,takeProfit:takeProfit?Number(takeProfit):undefined,stopLoss:stopLoss?Number(stopLoss):undefined})}),data=await r.json();if(!r.ok||!data.ok)throw new Error(data.error||"Position rejected");wallet.futures-=margin;setTestnetBalances(balances);addTestnetPosition(data.position);setPositions(getTestnetPositions());setQuantity("");setMessage(`Position opened · ${margin.toFixed(2)} USDT margin`)}catch(e){setMessage(e instanceof Error?e.message:"Position failed")}finally{setSubmitting(false)}}
 function selectMarket(next:string){setPair(next);window.history.replaceState(null,"",`/futures?pair=${encodeURIComponent(next.replace("/",""))}`)}
 return <main className="app-shell"><header className="appbar"><Link className="brand" href="/"><img src="/orbitex-logo.svg" alt="ORBITEX" style={{width:30,height:30,objectFit:"contain"}}/><span>ORBITEX.</span></Link><nav><Link href="/dashboard">Dashboard</Link><Link href="/trade">Spot</Link><Link className="active" href="/futures">Futures</Link><Link href="/wallet">Wallet</Link><Link href="/orders">Orders</Link></nav><Link className="btn" href="/dashboard">Account</Link></header><div className="futures-wrap"><div className="trade-head"><div><div className="pair"><CoinIcon symbol={base} size={28}/>{pair} <span className="perpetual-badge">Perpetual</span></div><div className="price">{markPrice>0?formatPrice(markPrice):"Loading live price…"} <span className="up">LIVE</span></div><div className="muted tiny">Public futures mark-price feed · testnet trading engine</div></div></div><div className={`market-list futures-market-list ${expanded?"expanded":""}`}>{MARKET_SYMBOLS.slice(0,30).map(m=>{const p=displayPair(m);return <button key={m} className={p===pair?"selected":""} onClick={()=>selectMarket(p)}><CoinIcon symbol={m.replace("USDT","")} size={20}/>{p}</button>})}</div><button type="button" className="futures-expand-button" onClick={()=>setExpanded(x=>!x)}>{expanded?"Collapse coins":"Expand all coins"}<span>{expanded?"⌃":"⌄"}</span></button><div className="futures-grid"><div className="futures-chart-area"><MarketChart pair={pair}/><section className="panel positions-panel"><div className="section-heading"><div><div className="label">OPEN POSITIONS</div><h2>Positions</h2></div><span className="muted">{positions.filter(p=>p.status==="OPEN").length} open</span></div>{positions.length===0?<p className="muted tiny">No positions yet. Open a futures position to see it here.</p>:positions.slice(0,8).map(p=><div key={p.id} className="position-card"><div className="position-row"><div><strong>{p.side} · {p.symbol}</strong><div className="muted tiny">Entry {p.entryPrice.toFixed(2)} · Mark {p.markPrice.toFixed(2)} · {p.leverage}x</div></div><div className="position-right"><strong>{(p.status==="OPEN"?p.unrealizedPnl:p.realizedPnl||0).toFixed(2)} USDT</strong><span className="muted tiny">{p.status||"OPEN"}</span></div></div></div>)}</section></div><aside className="order-panel"><div className="panel-title"><div><div className="label">FUTURES TRADING</div><h2>{pair}</h2></div><span className="muted tiny">Testnet</span></div><div className="control-grid"><label>Leverage<select value={lev} onChange={e=>setLev(Number(e.target.value))}>{[1,2,3,5,10,20,50,100].map(x=><option key={x}>{x}x</option>)}</select></label></div><div className="tabs"><button className={side==="Long"?"on long-tab":""} onClick={()=>setSide("Long")}>Open Long</button><button className={side==="Short"?"on short-tab":""} onClick={()=>setSide("Short")}>Open Short</button></div><div className="order-type-tabs"><button className={orderType==="Limit"?"on":""} onClick={()=>setOrderType("Limit")}>Limit</button><button className={orderType==="Market"?"on":""} onClick={()=>setOrderType("Market")}>Market</button></div>{orderType==="Limit"&&<label>Entry price<input value={entryPrice} onChange={e=>setEntryPrice(e.target.value)} inputMode="decimal" placeholder={markPrice>0?formatPrice(markPrice):"Loading live price…"}/></label>}<label>Quantity<input value={quantity} onChange={e=>setQuantity(e.target.value)} inputMode="decimal" placeholder={`Amount in ${base}`}/></label><div className="control-grid"><label>Take profit<input value={takeProfit} onChange={e=>setTakeProfit(e.target.value)} placeholder="Optional"/></label><label>Stop loss<input value={stopLoss} onChange={e=>setStopLoss(e.target.value)} placeholder="Optional"/></label></div><div className="order-summary"><div><span>Estimated margin</span><strong>{((Number(entryPrice)||0)*(Number(quantity)||0)/lev).toFixed(2)} USDT</strong></div><div><span>Live P&amp;L preview</span><strong>{livePnl.toFixed(2)} USDT</strong></div></div>{message&&<div className="notice">{message}</div>}<button className={`submit-futures ${side==="Long"?"long-submit":"short-submit"}`} disabled={submitting||markPrice<=0} onClick={submit}>{submitting?"Opening…":markPrice<=0?"Waiting for live price…":side==="Long"?"Open Long":"Open Short"}</button><p className="muted tiny disclaimer">Testnet only. Prices use a resilient Binance WebSocket and server fallback.</p></aside></div></div></main>;
}
