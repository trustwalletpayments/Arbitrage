"use client";
import {useState} from "react";import Link from "next/link";
export default function Futures(){const [side,setSide]=useState("Long");const [lev,setLev]=useState(10);return <main className="app-shell"><header className="appbar"><Link className="brand" href="/">ARBITRAGE<span>.</span></Link><nav><Link href="/dashboard">Dashboard</Link><Link href="/trade">Spot</Link><Link className="active" href="/futures">Futures</Link><Link href="/wallet">Wallet</Link></nav><Link className="btn" href="/">Account</Link></header><div className="futures-wrap"><div className="trade-head"><div><div className="pair">BTC/USDT Perpetual</div><div className="price">113,842.20 <span className="up">+2.14%</span></div></div><div className="leverage">Leverage <select value={lev} onChange={e=>setLev(Number(e.target.value))}>{[1,2,3,5,10,20,50,100].map(x=><option key={x}>{x}x</option>)}</select></div></div><div className="futures-grid"><div className="chart"><div className="chart-grid"></div><div className="chart-line">╱╲╱╲╱╲╱╲╱╲╱╲╱╲╱╲╱</div><span>Futures chart · live market data will be connected later</span></div><div className="order-panel"><div className="tabs"><button className={side==="Long"?"on":""} onClick={()=>setSide("Long")}>Long</button><button className={side==="Short"?"on":""} onClick={()=>setSide("Short")}>Short</button></div><label>Order type<select><option>Limit</option><option>Market</option></select></label><label>Entry price<input placeholder="USDT"/></label><label>Quantity<input placeholder="BTC"/></label><div className="two-input"><label>Take profit<input placeholder="Optional"/></label><label>Stop loss<input placeholder="Optional"/></label></div><div className="order-info"><span>Available margin</span><span>2,620.00 USDT</span></div><button className="btn primary full" onClick={()=>alert("Futures engine will be connected to the backend later.")}>{side} BTC</button><p className="muted tiny">Leverage: {lev}x · Testnet UI only. No real position is opened.</p></div></div></div></main>}
